apiVersion: v1
kind: Service
metadata:
  name: worker-test-nginx-app-ocp
  namespace: test
spec:
  ports:
  - name: web
    port: 8080
    protocol: TCP
    targetPort: 80
    nodePort: 32000
  selector:
    test: nginx-2
  type: NodePort
