apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-pvc
  namespace: openshift-image-registry
spec:
  storageClassName: registry-sc
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Gi
